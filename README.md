# python_coding_repo
Python coding challenges


git client setup:
1. should have git client in machine, if not install it.
2. configure the user name and email, details of git account using following commends.

> git config --global user.name "mounish-code"
> git config --global user.email "mchelimilla@gmail.com"

3. create a folder in your laptop to import git repo:
> mkdir python
> cd python

4.  
 To clone the repository and initiate git using following commands, copy the repo's https URL from repo:
> git init
> git clone https://github.com/mounish-code/python_coding_repo.git
